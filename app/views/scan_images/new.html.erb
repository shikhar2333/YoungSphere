<h1>New Scan Image</h1>
<div class="field">
    <%= form_with(model: @scan_image, local: true) do |f| %>
        <%= f.file_field :scanned_image %>
        <%= f.submit "Upload", class: "btn btn-primary" %>
    <% end %>   
</div>
